[project]
name = "cryptofeed-clickhouse"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cryptofeed==2.4.1",
    "orjson",
    "websockets",
    "questdb",
    "clickhouse-driver",
]
